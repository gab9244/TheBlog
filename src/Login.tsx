
const Login = () => {
  return (
    <div className="Login">
      <input type="text" placeholder="UserName" />
      <input type="password"  placeholder="PassWord "/>
      <button>Login</button>
    </div>
  );
};

export default Login;
